import assert from "assert";

describe("Testing parameterization", function () {
    const tests = [
        {
            earned: 30,
            spent: 10,
            expected: 20
        },
        {
            earned: 20,
            spent: 2,
            expected: 18
        }
    ];

    tests.forEach( ({earned, spent, expected}) => {
        
    });
});   
